import React from 'react';
import { DatePicker, Button } from 'antd';

const App = () => (
    <div>
        <Button type="primary">测试antd</Button>
        <DatePicker />
    </div>
);
export default App;
